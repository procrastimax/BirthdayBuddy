<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                             xmlns:app="http://schemas.android.com/apk/res-auto"
                                             android:layout_width="match_parent"
                                             android:layout_height="?attr/actionBarSize">

    <TextView
            android:text="@string/app_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/tv_title"
            android:layout_marginEnd="8dp"
            android:layout_marginStart="8dp"
            app:layout_constraintBottom_toBottomOf="@+id/iv_toolbar_show_event_back"
            app:layout_constraintHorizontal_bias="0.05"
            android:textSize="@dimen/larg_text_size"
            android:textColor="@color/darkGrey"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@+id/iv_toolbar_show_event_back"
            app:layout_constraintEnd_toStartOf="@+id/iv_toolbar_show_event_share"
            app:layout_constraintTop_toTopOf="@+id/iv_toolbar_show_event_back"/>

    <ImageView
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:srcCompat="@drawable/ic_edit"
            android:id="@+id/iv_toolbar_show_event_edit"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginEnd="16dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:clickable="true"
            android:focusable="true"
            android:theme="@style/SelectableItemTheme"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintDimensionRatio="1:1"/>

    <ImageView
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:srcCompat="@drawable/ic_arrow_back"
            android:id="@+id/iv_toolbar_show_event_back"
            app:layout_constraintTop_toTopOf="parent"
            android:clickable="true"
            android:focusable="true"
            android:theme="@style/SelectableItemTheme"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="8dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            app:layout_constraintDimensionRatio="1:1"/>

    <ImageView
            android:layout_height="0dp"
            app:srcCompat="@drawable/ic_share"
            android:id="@+id/iv_toolbar_show_event_share"
            app:layout_constraintEnd_toStartOf="@+id/iv_toolbar_show_event_edit"
            android:layout_marginEnd="24dp"
            android:clickable="true"
            android:focusable="true"
            android:theme="@style/SelectableItemTheme"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintTop_toTopOf="@+id/iv_toolbar_show_event_edit"
            app:layout_constraintBottom_toBottomOf="@+id/iv_toolbar_show_event_edit"
            app:layout_constraintDimensionRatio="1:1"
            android:layout_width="0dp"/>
</android.support.constraint.ConstraintLayout>